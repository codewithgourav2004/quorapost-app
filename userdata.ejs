<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Stored User Info</title>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #3498db, #2ecc71);
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            color: black;
        }

        h2 {
            text-align: center;
            margin-bottom: 20px;
            
        }

        table {
            width: 80%;
            border-collapse: collapse;
            margin: 20px auto;
            background-color: rgba(255, 255, 255, 0.1);
            border-radius: 12px;
            box-shadow: 0 8px 24px rgba(0, 0, 0, 0.3);
            backdrop-filter: blur(10px);
        }

        th, td {
            padding: 12px;
            border: 1px solid #fff;
            text-align: center;
        }

        th {
            background-color: rgba(255, 255, 255, 0.3);
        }

        tr:hover {
            background-color: rgba(255, 255, 255, 0.2);
        }

        .no-users {
            font-size: 18px;
            text-align: center;
            color: #f1c40f;
        }

    </style>
</head>
<body>

    <div class="container">
        <h2>Registered Users</h2>

        <% if (users.length === 0) { %>
            <p class="no-users">No users have signed up yet.</p>
        <% } else { %>
            <table>
                <tr>
                    <th>Username</th>
                    <th>Email</th>
                    <th>Password</th>
                </tr>
                <% users.forEach(user => { %>
                    <tr>
                        <td><%= user.username %></td>
                        <td><%= user.email %></td>
                        <td><%= user.password %></td>
                    </tr>
                <% }); %>
            </table>
        <% } %>
    </div>

</body>
</html>
